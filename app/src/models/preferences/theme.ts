interface Theme {
    uuid: string;
    name: string;
    primary: string;
    primary_variant: string;
    secondary: string;
    secondary_variant: string;
    accent: string;
    accent_variant: string;
    background: string;
    background_variant: string;
}